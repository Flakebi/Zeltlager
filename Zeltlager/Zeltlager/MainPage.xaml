<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Zeltlager"
             x:Class="Zeltlager.MainPage"
             Title="Zeltlager"
		 	 Style="{StaticResource BaseStyle}">
	<ContentPage.ToolbarItems>
		<ToolbarItem x:Name="settingsButton" Icon="{x:Static local:Icons.SETTINGS}" Clicked="OnSettingsClicked">
		</ToolbarItem>
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<StackLayout VerticalOptions="FillAndExpand"
					 HorizontalOptions="FillAndExpand"
					 Orientation="Vertical"
					 Spacing="15"
					 Padding="10,10,10,0">
			<Button x:Name="synchronizeButton" Text="Synchronisieren" Clicked="OnSynchronizeClicked" Style="{StaticResource DarkButtonStyle}"/>
			<Button x:Name="competitionButton" Text="Wettkämpfe" Clicked="OnCompetitionClicked" Style="{StaticResource DarkButtonStyle}"/>
			<Button x:Name="erwischtButton" Text="Erwischt" Clicked="OnErwischtClicked" Style="{StaticResource DarkButtonStyle}"/>
			<Button x:Name="calendarButton" Text="Kalender" Clicked="OnCalendarClicked" Style="{StaticResource DarkButtonStyle}"/>
			<Button x:Name="generalButton" Text="Allgemeines" Clicked="OnGeneralClicked" Style="{StaticResource DarkButtonStyle}"/>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>