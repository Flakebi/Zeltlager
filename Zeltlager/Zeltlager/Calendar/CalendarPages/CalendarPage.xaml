<?xml version="1.0" encoding="UTF-8"?>
<CarouselPage xmlns="http://xamarin.com/schemas/2014/forms"
              xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:local="clr-namespace:Zeltlager;assembly=Zeltlager"
              x:Class="Zeltlager.Calendar.CalendarPage"
              Title="Kalender"
              Style="{StaticResource BaseStyle}">
  <CarouselPage.ToolbarItems>
    <ToolbarItem x:Name="AddButton" Icon="{x:Static local:Icons.ADD}" Clicked="OnAddButtonClicked">
    </ToolbarItem>
    <ToolbarItem x:Name="RecurrentButton" Icon="{x:Static local:Icons.FOOD}" Clicked="OnRecurrentButtonClicked">
    </ToolbarItem>
    <ToolbarItem x:Name="PlannedButton" Icon="{x:Static local:Icons.PLAN}" Clicked="OnPlannedButtonClicked">
    </ToolbarItem>
  </CarouselPage.ToolbarItems>
</CarouselPage>